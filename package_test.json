{
  "scripts": {
    "test": "jest --forceExit --coverage --verbose",
    "watch-test": "npm run test -- --watchAll"
  },
  "devDependencies": {
    "@types/chai": "^4.2.3",
    "@types/jest": "^24.0.18",
    "@types/supertest": "^2.0.8",
    "chai": "^4.2.0",
    "jest": "^24.9.0",
    "supertest": "^4.0.2",
    "ts-jest": "^24.1.0"
  }
}
